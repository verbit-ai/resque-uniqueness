RSpec.describe Resque::Scheduler::Unique::Jobs do
  it "has a version number" do
    expect(Resque::Scheduler::Unique::Jobs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
